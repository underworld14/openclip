/**
 * Dashboard — recent projects + import entry (STUB; owned by T-Persist, E.3).
 * PRD §11.2. The trunk ships the seam; the track replaces only this body.
 */
export function Dashboard(): React.JSX.Element {
  return (
    <div data-testid="dashboard" className="p-3 text-sm text-muted-foreground">
      Dashboard (recent projects) — stub
    </div>
  )
}

export default Dashboard
