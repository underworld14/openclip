/**
 * ExportPanel — quality/format + export action (STUB; owned by the export
 * spine, E.5). PRD §6.9 / §11.2. Trunk ships the seam only.
 */
export function ExportPanel(): React.JSX.Element {
  return (
    <div data-testid="export-panel" className="p-3 text-sm text-muted-foreground">
      Export (quality / format) — stub
    </div>
  )
}

export default ExportPanel
