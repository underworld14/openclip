/**
 * ImportPanel — drag/drop + URL import + progress (STUB; owned by T-Media, E.3).
 * PRD §6.1 / §11.2. The trunk ships the seam; the track replaces only this body.
 */
export function ImportPanel(): React.JSX.Element {
  return (
    <div data-testid="import-panel" className="p-3 text-sm text-muted-foreground">
      Import video (drag-drop / URL) — stub
    </div>
  )
}

export default ImportPanel
