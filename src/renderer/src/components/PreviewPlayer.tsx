/**
 * PreviewPlayer — HTML5 <video> preview of the source (STUB; owned by the
 * timeline spine, E.5). PRD §6.6 / §11.1 (center column). Trunk ships seam only.
 */
export function PreviewPlayer(): React.JSX.Element {
  return (
    <div
      data-testid="preview-player"
      className="flex aspect-video w-full items-center justify-center rounded-md bg-black/80 text-sm text-white/60"
    >
      Preview player — stub
    </div>
  )
}

export default PreviewPlayer
