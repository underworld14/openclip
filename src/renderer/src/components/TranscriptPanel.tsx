/**
 * TranscriptPanel — searchable transcript w/ timestamps (STUB; T-Media, E.3).
 * PRD §6.2 / §11.2. The trunk ships the seam; the track replaces only this body.
 */
export function TranscriptPanel(): React.JSX.Element {
  return (
    <div data-testid="transcript-panel" className="p-3 text-sm text-muted-foreground">
      Transcript (searchable) — stub
    </div>
  )
}

export default TranscriptPanel
